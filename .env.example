GEMINI_API_KEY=
TIKTOK_CLIENT_KEY=
TIKTOK_CLIENT_SECRET=
META_APP_ID=
META_APP_SECRET=
INSTAGRAM_APP_ID=
INSTAGRAM_APP_SECRET=
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
APP_URL=

# Set to "true" to require Creator OS login (signup/login screen) before the
# app loads. Off by default — the app works exactly as before with no login.
REQUIRE_AUTH=
# Secret used to sign session cookies. Only matters once REQUIRE_AUTH=true;
# set this to a long random string before turning auth on.
SESSION_SECRET=
